services:
  - type: cron
    name: flight-data-collector
    runtime: python
    schedule: "*/10 * * * *"
    buildCommand: pip install -r requirements.txt
    startCommand: python "Data Collection + Processing/collect_all.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
